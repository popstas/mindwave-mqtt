import { defineComponent, PropType, ref, withKeys, withModifiers } from "vue";

export default defineComponent({
  name: "DaysList",
  props: {
  },
  setup(props) {
    return () => (
      <div>
        DaysList
      </div>
    );
  },
});
